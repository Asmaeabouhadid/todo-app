<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"
        android:text="Bienvenue ! Notre Application de Gestion des Tâches Personnelles et en Équipe"
        android:textAlignment="center"
        android:textColor="@color/purple_200"
        android:textSize="15sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/imageViewLogo"
        android:layout_width="323dp"
        android:layout_height="330dp"
        android:layout_below="@id/textViewTitle"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:src="@drawable/todo" />

    <TextView
        android:id="@+id/textViewCreateProfile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/imageViewLogo"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Si c'est votre première fois dans notre équipe, créez votre profil ici pour ajouter votre propre équipe"
        android:textColor="@color/black" />

    <LinearLayout
        android:layout_width="395dp"
        android:layout_height="133dp"
        android:layout_below="@id/textViewCreateProfile"
        android:layout_marginTop="12dp"
        android:gravity="center_horizontal"
        android:orientation="horizontal"
        android:padding="16dp">

        <Button
            android:id="@+id/buttonUserLogin"
            android:layout_width="0dp"
            android:layout_height="61dp"
            android:layout_weight="1"
            android:backgroundTint="@color/purple_200"
            android:elevation="4dp"
            android:text="User "
            android:textColor="@android:color/white"
            android:textSize="18sp" />

        <Button
            android:id="@+id/buttonAdminLogin"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_weight="1"
            android:backgroundTint="@color/purple_200"
            android:elevation="4dp"
            android:text="Admin "
            android:textColor="@android:color/white"
            android:textSize="18sp" />

    </LinearLayout>

</RelativeLayout>
